export interface UserState {
  isLogined: boolean;
  avatar: string;
  nickname: string;
  userId: string;
}
export interface LoginState {
  isLogined: boolean;
  avatar: string;
  nickname: string;
  userId: string;
}

export interface UpdateUser {
  avatar: string;
}
